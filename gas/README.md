# 成果接收服務

Apps Script 已儲存至 `deployment.json` 指定的綁定專案，目標為使用者指定試算表的 gid=0。已完成部署，正式網址已寫入本地 `.env.local`。2026-09-07 實測公開健康檢查與本地 API 新增、相同請求重試、同一學生更新全部成功，並在試算表確認第 2 列積分更新為 2；保留一列明確標示的非學生驗收資料。

部署採執行身分「我」、存取「所有人」。端點只接受已驗證的 POST，驗證碼保留在本地 `.env.local`，不傳至學生瀏覽器。GET 僅回傳服務狀態，不讀取學生資料。試算表本身不公開共用。

`Code.gs` 的 `__UPLOAD_SECRET__` 是供保存原始碼使用的替代文字，雲端專案已填入本次本地產生的值。部署成功後將 `/exec` URL 寫入 `.env.local` 的 `GAS_DEPLOYMENT_URL`，並更新 `deployment.json`。不得使用 `/dev` URL。

相同班級、座號、姓名會更新同一列，相同 requestId 重試不重複寫入。採用 script lock 避免同時寫入互相覆蓋。當既有工作表第一列不是本服務欄位時拒絕寫入，保護原有資料。

分數分開記錄：學習積分 24、首次／最高／修正評量各 12、遊戲最高 6、已完成學習頁數 7。未作答的評量和遊戲以空白保存。第 08 頁不是學習任務，不列入七頁完成分母。

分頁解鎖是本地課堂導覽功能，不是帳戶登入或正式考試的防作弊機制。密碼由 `/api/unlock` 比對，學生網頁不包含明文密碼；解鎖結果保存在此瀏覽器，並可恢復依序解鎖。
